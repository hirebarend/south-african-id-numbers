{
  "name": "south-african-id-numbers",
  "version": "0.2.8",
  "description": "",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "prettier --write --config prettier.config.js --list-different src/**/*.ts",
    "start": "npm run build && node dist/main.js",
    "test": "npm run build && mocha --timeout 5000 dist/**/*.test.js"
  },
  "author": "Barend Erasmus",
  "license": "MIT",
  "devDependencies": {
    "@types/assert": "^1.5.11",
    "@types/css": "^0.0.38",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.9.2",
    "assert": "^2.1.0",
    "mocha": "^10.8.2",
    "prettier": "^3.3.3",
    "tslib": "^2.8.1",
    "typescript": "^5.7.2"
  }
}
